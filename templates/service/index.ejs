<%_
  var { name, pb_package,serviceList,messageTypeList } = proto;
_%>
/**
 * build from <%=name%>
 * 自动生成 请勿修改
 * <%= new Date().toString()%>
 */

import { Service } from 'egg'
<%_ var fileNameArr=fileName.split('')
fileNameArr[0]=fileNameArr[0].toUpperCase()
var upperFileName=fileNameArr.join('') _%>
<%- include('/ts',{proto})%>
export default class <%= upperFileName%>Service extends Service {
      <%_ for (const service of serviceList){ _%>
          <%_ const {name:serviceName , methodList}=service _%>
                <%- include('/method',{ proto:{ methodList,fileName,serviceName}})_%>
      <%_ } _%>  
}
